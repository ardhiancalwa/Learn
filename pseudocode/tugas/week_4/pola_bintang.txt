function polaBintang(n, i, j : integer, result : string) -> string
{
    mengembalikan nilai result yang menampilkan pola bintang sesuai n masukan dari user
}
algoritma
    if i > n then
        return result
    endif
    if i > j then
        return polaBintang(n, i, j+1, result+"*")
    else
        return polaBintang(n, i+1, 0, result+newline)
    endif
endfunction